[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crypto-pipeline"
version = "0.1.0"
description = "Real-time crypto trade streaming and analytics platform"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
